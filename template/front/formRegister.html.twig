{% extends "layout.html.twig" %}
{% block content %}
    <h3 class="mb-5 text-uppercase">
        <span class="fas fa-file-signature"></span>
        Inscription :
    </h3>
    <div class="container mb-5">
        {% if status == true %}
            <div class="alert alert-{{ status }}">{{ statusMessage }}</div>
        {% endif %}
        <form action="/register/add" method="POST" class="needs-validation p-4 starter-template rounded shadow"
              novalidate>
            <div class="col">
                {{ include('partial/_formRegister.html.twig') }}
                <button type="submit" class="p-2 mt-4 btn btn-success">
                    Envoyer
                    <span class="fas fa-share"></span>
                </button>
            </div>
        </form>
    </div>
    <div class="p-3 mt-4 mb-5 mx-auto w-50 border-top border-bottom border-info">
        <h4>Déjà inscrit ?</h4>
        <a href="/authentication" class="btn btn-primary p-2 my-2">
            <span class="fas fa-sign-in-alt"></span>
            Connection
        </a>
    </div>
{% endblock %}

